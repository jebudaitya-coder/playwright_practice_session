# Playwright Practice Session

This workspace is set up for a first Playwright Test using Node.js.

Quick start

```powershell
cd C:\Playwright_projects\Personal_projects\playwright_practice_session
npm install
npx playwright install
npm test
```

Files

- `tests/example.spec.js`: sample test visiting https://example.com
- `playwright.config.js`: Playwright Test configuration
